//table login:
enter
enter
enter
enter
password
\c dcdc_tests;

// can kill table with this command set, but please dont
DROP TABLE dc_dc_tests;
DROP TABLE board_traces;

// delete cold tests for this list with a given phase
UPDATE dc_dc_tests
SET
    secondary_calibration = NULL,
    initial_cold_voltage = NULL,
    initial_cold_current = NULL,
    input_current_output_voltage = NULL,
    output_step_load = NULL,
    input_step_voltage = NULL,
    cold_start_up = NULL,
    voltage_dev_cold = NULL,
    mc_ave_vol_c = NULL,
    mc_ave_cur_c = NULL
WHERE phase = 'YOUR_PHASE'
  AND board_id IN ('001', '002', '003');

//add new cols
ALTER TABLE dc_dc_tests
ADD column TYPE;

//rename col
ALTER TABLE board_traces
RENAME COLUMN old_name TO new_name;